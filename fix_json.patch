--- resources/views/orders/create.blade.php
+++ resources/views/orders/create.blade.php
@@ -2090,5 +2090,5 @@
         cart: [], couponCode: '', couponApplied: false, appliedCouponObj: null, appliedOfferId: null,
         placing: false, formErrors: [],
         warehouses: @json($warehouses->map(fn($w) => ['id' => $w->id, 'name' => $w->name])),
-        activeOffers: @json($activeOffers->map(fn($o) => array_merge($o->toArray(), ['product_name' => $o->product?->name ?? '']))),
-        activeCoupons: @json($activeCoupons),
+        activeOffers: @json($activeOffers->values()->map(fn($o) => array_merge($o->toArray(), ['product_name' => $o->product?->name ?? '']))),
+        activeCoupons: @json($activeCoupons->values()),
